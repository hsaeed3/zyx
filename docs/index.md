# zyx - a fun & expressive sdk for context engineering

### Installation

Install `zyx` via pip, or even better through `uv`.

```bash
pip install zyx

# or use uv
# uv pip install zyx
# uv add zyx
```

